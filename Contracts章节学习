数据类型-合约
合约包括状态变量、函数、修饰符等，其中状态变量都是存储在区块链上的数据；函数是无状态的，所以其中不可能定义原生的storage变量？还是说可以定义storage变量，然后赋值给状态变量，但storage之间赋值不进行内容拷贝，状态变量引用了函数内部变量，由此来看，这是不允许的，
在函数中定义的引用类型变量，storage变量只能
1、智能合约构造函数：
  关键字constructor
2、mapping数据的存储位置只能为storage,且只能在状态变量位置进行定义，函数内部的mapping必须要引用状态变量的map，不能动态创建
